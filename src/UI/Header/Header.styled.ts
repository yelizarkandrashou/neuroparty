import styled from 'styled-components';

const Header = styled.span`
  font-family: VinSlabPro;
  font-size: 30px;
  font-style: normal;
  font-weight: 400;
  line-height: 31px;
  letter-spacing: 0;
  text-align: left;
  margin-bottom: 48px;
`;

export default {
  Header,
};
